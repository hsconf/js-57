export interface User {
    id: string;
    name: string;
    email: string;
    status: boolean;
    role: string;
}

export interface UserMute {
    id: string;
    name: string;
    email: string;
    role: string;
    status: boolean;
}